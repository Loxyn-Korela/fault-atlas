{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://faultatlas.loxyn.ai/schema/form.schema.json",
  "title": "Fault Atlas — form record",
  "description": "One observed form of fault in a knowledge graph built from documents. The file is the record of truth: it must be readable and judgeable without any software.",
  "type": "object",
  "required": [
    "id",
    "version",
    "name",
    "damage",
    "class",
    "layer",
    "status",
    "seen",
    "specimens",
    "prevention",
    "repair",
    "judgeable_by",
    "history",
    "origin"
  ],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^form-[0-9]{3}[a-z]?$",
      "description": "Stable identifier. Never reused."
    },
    "version": {
      "type": "integer",
      "minimum": 1
    },
    "legacy_line": {
      "type": "integer",
      "description": "Line number in the catalogue of 2026-08-09 (korela.db), for traceability."
    },
    "name": {
      "type": "string",
      "minLength": 3
    },
    "name_fr": {
      "type": "string",
      "description": "Original French name, verbatim from the source catalogue."
    },
    "damage": {
      "type": "string",
      "enum": [
        "MERGE",
        "SPLIT",
        "SPURIOUS_EDGE",
        "MISSING",
        "WRONG_VALUE",
        "WRONG_LABEL",
        "ANACHRONISM",
        "CORPUS_PARAMETER"
      ],
      "description": "What the form does to the graph. MERGE: two real things become one node. SPLIT: one thing becomes several nodes. SPURIOUS_EDGE: a relation that the source never asserted. MISSING: a node or relation that the source asserts and the graph lacks. WRONG_VALUE: a property, number or date is wrong. WRONG_LABEL: an entity or relation type is wrong. ANACHRONISM: a fact is placed outside its validity interval, or a superseded status is kept. CORPUS_PARAMETER: not a damage; a property of the corpus composition."
    },
    "class": {
      "type": "string",
      "enum": [
        "DEFEATED",
        "DEFEATED_IF_XML",
        "REFUSABLE",
        "SILENT_FALSE",
        "IRREDUCIBLE",
        "UNCLASSIFIED"
      ],
      "description": "DEFEATED: cancelled by deterministic code, no judgement of meaning, with a test. DEFEATED_IF_XML: cancelled when the source is structured (JATS/XML), not on composed PDF. REFUSABLE: code can either handle it or explicitly abstain; never produces a false. SILENT_FALSE: the error yields a plausible result that code cannot detect from inside. IRREDUCIBLE: requires meaning; no code cancels it."
    },
    "layer": {
      "type": "string",
      "description": "Layer of the product stack where the form bites (korela_base 00b numbering kept as text)."
    },
    "injection": {
      "type": "string",
      "enum": [
        "RENAME",
        "DUPLICATE",
        "DELETE",
        "ALTER",
        "NOISE",
        "MOVE",
        "INSERT",
        "FORMAT",
        "TRANSLATE",
        "IMAGE",
        "DESIGN",
        "CONSTRAINT",
        "PARAMETER"
      ],
      "description": "How the form is fabricated into a document for a controlled corpus. IMAGE requires rendering and vision, not text injection. DESIGN: decided in the truth (world specification), not injected. CONSTRAINT: a schema rule."
    },
    "legacy_severity": {
      "type": "string",
      "description": "G1..G4 of the 2026-08 catalogue. House vocabulary, kept for traceability only; not a measure."
    },
    "legacy_mechanism": {
      "type": "string",
      "description": "M1..M12 remedy family of the 2026-08 catalogue. Traceability only."
    },
    "status": {
      "type": "string",
      "enum": [
        "proposed",
        "validated",
        "refuted",
        "migrated_unreviewed"
      ]
    },
    "observed_absent_in": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Corpora where the form was searched for and measured absent (a result, not a gap)."
    },
    "seen": {
      "type": "array",
      "description": "Where the form was observed. Empty array = no specimen yet; the form is then a hypothesis, not an observation.",
      "items": {
        "type": "object",
        "required": [
          "corpus",
          "date",
          "excerpt"
        ],
        "additionalProperties": false,
        "properties": {
          "corpus": {
            "type": "string"
          },
          "document": {
            "type": "string"
          },
          "date": {
            "type": "string",
            "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$"
          },
          "observer": {
            "type": "string"
          },
          "excerpt": {
            "type": "string",
            "minLength": 1,
            "description": "Verbatim, in the source language."
          },
          "lang": {
            "type": "string"
          },
          "excerpt_en": {
            "type": "string",
            "description": "English translation of the excerpt when the original is not in English. The original stays the record; the translation is a courtesy."
          },
          "organisation": {
            "type": "string",
            "description": "Organisation of the observer, when different from the form's origin."
          },
          "corpus_id": {
            "type": "string",
            "description": "Identifier of a corpus record in corpora/, when the observation was made on a published, reproducible list."
          },
          "probe": {
            "type": "string",
            "description": "Path, in this repository, of the exact probe that produced the figures of this observation (see the form's probes list). One observation, one probe."
          },
          "reserve": {
            "type": "string",
            "description": "A stated limit of this observation (for example: the identifiers of the documents were not kept, so the observation is cited, not reproducible). Put on the observation itself, never only in the history."
          }
        }
      }
    },
    "specimens": {
      "type": "object",
      "required": [
        "cases",
        "counter_examples"
      ],
      "additionalProperties": false,
      "properties": {
        "cases": {
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "input",
              "expected"
            ],
            "properties": {
              "input": {},
              "expected": {},
              "note": {
                "type": "string"
              }
            }
          }
        },
        "counter_examples": {
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "input",
              "expected"
            ],
            "properties": {
              "input": {},
              "expected": {},
              "note": {
                "type": "string"
              }
            }
          },
          "description": "What a remedy must NOT touch. A form with cases and no counter-example cannot be tested."
        }
      }
    },
    "prevention": {
      "type": "object",
      "required": [
        "cancellable_by_code"
      ],
      "additionalProperties": false,
      "properties": {
        "cancellable_by_code": {
          "type": "boolean"
        },
        "refusal_clause": {
          "type": "string",
          "description": "When the code cannot conclude, what it returns instead of guessing."
        },
        "needs": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "External registries or identifiers required (Crossref, ORCID, trial registries...)."
        },
        "note": {
          "type": "string"
        }
      }
    },
    "construction": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "guard_effect": {
          "type": "string",
          "description": "Measured effect of the guard that holds this form, by outcome only. Never the mechanism."
        },
        "source": {
          "type": "string"
        }
      }
    },
    "repair": {
      "type": "object",
      "required": [
        "reachable_by_deletion"
      ],
      "additionalProperties": false,
      "properties": {
        "measured": {
          "type": "object",
          "description": "What a deletion-only repair actually reached for this damage class, measured on the bench rather than judged. It sits beside reachable_by_deletion and never replaces it: where the two disagree, the disagreement is the result. Per damage class, not per form.",
          "required": ["value", "reached", "injected", "source", "date"],
          "additionalProperties": false,
          "properties": {
            "value": {"type": "string", "enum": ["yes", "no", "partial", "yes_at_a_cost", "depends_on_visibility"]},
            "reached": {"type": "integer", "minimum": 0},
            "injected": {"type": "integer", "minimum": 0},
            "note": {"type": "string"},
            "source": {"type": "string"},
            "date": {"type": "string", "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$"},
            "disagrees_with_the_judgment": {"type": "string"}
          }
        },
        "reachable_by_deletion": {
          "type": "string",
          "enum": [
            "yes",
            "no",
            "partial",
            "unknown"
          ],
          "description": "Can a deletion-only repair (nodes, edges, labels) restore the truth for this form?"
        },
        "note": {
          "type": "string"
        }
      }
    },
    "judgeable_by": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "construction",
          "registry",
          "curated",
          "dated_future",
          "closed_world"
        ]
      },
      "description": "Which kind of truth can judge this form. Empty = no known truth judges it yet."
    },
    "observer_agreement": {
      "type": "string"
    },
    "related": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "history": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "date",
          "event"
        ],
        "properties": {
          "date": {
            "type": "string"
          },
          "event": {
            "type": "string"
          },
          "by": {
            "type": "string"
          }
        }
      }
    },
    "origin": {
      "type": "object",
      "required": [
        "organisation"
      ],
      "additionalProperties": false,
      "properties": {
        "organisation": {
          "type": "string",
          "description": "Organisation that contributed the form (e.g. 'Loxyn SAS, Lyon' or 'LIRIS, CNRS/Lyon 1')."
        },
        "campaign": {
          "type": "string",
          "description": "Observation campaign or project the form comes from."
        },
        "contact": {
          "type": "string",
          "description": "Public contact of the contributing organisation."
        }
      }
    },
    "probes": {
      "type": "array",
      "description": "The exact questions asked of a corpus, kept verbatim so that anyone can ask them again: a Python regex probe, a SPARQL query, a shell request. A probe repairs nothing and decides nothing; it answers 'is this fault here, and where?'.",
      "items": {
        "type": "object",
        "required": [
          "file",
          "kind",
          "corpus_id"
        ],
        "additionalProperties": false,
        "properties": {
          "file": {
            "type": "string",
            "description": "Path in this repository (probes/...). The file is the record; it carries its own header, endpoint and expected answer."
          },
          "kind": {
            "type": "string",
            "enum": [
              "python",
              "sparql",
              "http",
              "manual"
            ]
          },
          "corpus_id": {
            "type": "string",
            "description": "Corpus record in corpora/ the probe was run on."
          },
          "written": {
            "type": "string",
            "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$"
          },
          "run": {
            "type": "string",
            "description": "The command, verbatim."
          },
          "result": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "date",
              "count",
              "of"
            ],
            "properties": {
              "date": {
                "type": "string",
                "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$"
              },
              "count": {
                "type": "integer",
                "minimum": 0,
                "description": "Carrier documents (document probe) or hits (corpus probe, SPARQL rows)."
              },
              "of": {
                "type": [
                  "integer",
                  "null"
                ],
                "description": "Population size; null when the probe is a live query on a moving registry."
              },
              "strata": {
                "type": "object",
                "additionalProperties": {
                  "type": "integer"
                }
              },
              "examples": {
                "type": "array",
                "items": {
                  "type": "array",
                  "items": {},
                  "minItems": 1,
                  "maxItems": 2
                },
                "description": "Up to twelve carrier documents with their evidence; the full list is in carriers_file."
              },
              "by": {
                "type": "string"
              },
              "carriers_file": {
                "type": "string",
                "description": "File in probes/results/ listing every carrier document with the evidence found in it, so that the fault can be seen document by document."
              }
            }
          },
          "matches_excerpt": {
            "type": "boolean",
            "description": "True when the re-run figure is the figure quoted in the observation's excerpt. False is published as false."
          },
          "note": {
            "type": "string"
          }
        }
      }
    },
    "failure_mode": {
      "type": "object",
      "description": "How the form fails, from the internal catalogue of 2026-08-09. This is a second axis, independent of the damage: it is judged line by line and it varies inside a damage class, where repair.reachable_by_deletion does not. Forms added after the migration do not carry it yet.",
      "required": ["value", "means", "source"],
      "additionalProperties": false,
      "properties": {
        "value": {
          "type": "string",
          "enum": ["PROP", "FAUX", "MANQ", "MES", "DEC"],
          "description": "PROP: identity, the error lands on a node and propagates. FAUX: a false fact. MANQ: a gap, incomplete rather than wrong. MES: a property of the corpus or of the measuring apparatus. DEC: a use made downstream of the graph."
        },
        "means": {"type": "string"},
        "source": {"type": "string"}
      }
    },
    "era": {
      "type": "object",
      "description": "When the form applies. A dead difficulty is never thrown away: it takes the domain 'ancient' and serves whoever reads archives. Measured, never assumed.",
      "required": [
        "value",
        "measured",
        "source"
      ],
      "additionalProperties": false,
      "properties": {
        "value": {
          "type": "string",
          "enum": [
            "ancient_only",
            "both",
            "born_modern",
            "not_settled"
          ],
          "description": "ancient_only: gone from current documents (a workshop convention that disappeared). both: behaves the same in 1957 and in 2026. born_modern: has no ancestor, it came with digital publishing. not_settled: the sample was too small to say."
        },
        "measured": {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$"
        },
        "source": {
          "type": "string",
          "description": "The campaign that settled it, with its corpus and its own stated limits."
        },
        "note": {
          "type": "string"
        },
        "aggravated_by_the_modern": {
          "type": "boolean",
          "description": "Crosses both eras and is worse now than it was."
        },
        "discipline_reserve": {
          "type": "string",
          "description": "The era is not the only axis. When the form was only ever measured inside one discipline, name it here: a form that is biomedical before it is modern will make a corpus that resembles PubMed if it is taken as a generic property of the modern document. Measured on 2026-08-08: 14 articles of physics, mathematics, computer science, economics and linguistics carried zero structured abstract, zero pre-registration and zero ethics declaration."
        }
      }
    }
  }
}
